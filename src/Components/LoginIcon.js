import React from "react";
import "../App.css";
import avatarImage from "../images/avatarImage.png";

const LoginIcon = () => {
  return <img className="avatarImage" src={avatarImage} />;
};

export default LoginIcon;
